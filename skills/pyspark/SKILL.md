---
name: pyspark
description: PySpark programming in Databricks
---
Include everything in skill `python`.

Do not safe guard against missing dependencies etc. but let the code crash loudly if missing.

Use DataFrameWriterV2.append() rather than DataFrameWriter.insertInto().
